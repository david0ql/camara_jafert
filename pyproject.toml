[project]
name = "camara-jafert"
version = "0.1.0"
description = "Visor y fotómetro RGB para cámaras Thorlabs DCx (DCU224C)"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "numpy>=1.26",
    "opencv-python>=4.9",
]

[dependency-groups]
dev = ["pytest>=8.0"]

[tool.uv]
package = false
